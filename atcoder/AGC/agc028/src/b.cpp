#include <bits/stdc++.h>
using namespace std;
using lli = long long int;

int main()
{
    cin.tie(0);
    ios::sync_with_stdio(false);

    const lli MOD = 1000000000 + 7;

    int N;
    cin >> N;

    vector<lli> a(N);
    for (int i = 0; i < N; ++i) {
        cin >> a[i];
    }



    return 0;
}
